# MCP Server Configuration

# Server hostname/FQDN (for documentation purposes)
MCP_HOST=your-server.example.com
MCP_IP=192.168.1.100

# Port to listen on (HTTPS)
MCP_PORT=8443

# Authentication token (generate with: openssl rand -base64 32)
MCP_TOKEN=your_secure_token_here

# Set to 'false' to use HTTP (for development only)
USE_HTTPS=true

# SSL certificate paths (e.g., Let's Encrypt)
SSL_CERT_PATH=/etc/letsencrypt/live/your-server.example.com/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/your-server.example.com/privkey.pem

# Obsidian vault path (optional - leave empty if not using)
# OBSIDIAN_VAULT_PATH=/path/to/your/obsidian/vault

# Allowed paths for filesystem tools (comma-separated)
ALLOWED_PATHS=/home/user/projects,/home/user/documents

# SQLite database path (optional - leave empty if not using)
# DB_PATH=/path/to/your/database.sqlite

# UniFi Dream Machine configuration (optional - leave empty if not using)
# UNIFI_HOST=192.168.1.1
# UNIFI_API_KEY=your_unifi_api_key_here
# UNIFI_SITE=default
